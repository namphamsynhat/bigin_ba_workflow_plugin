---
type: pain-points-register
updated:
---

# Pain Point Register

One row per pain point across every feature — the canonical `PP-###` sequence. Each feature's hub mirrors its own rows into its `## Pain Points` section (`PP-###` and `Statement`/`Status`/`Proposed solution`/`Resolved by` stay identical in both places; `Feature` only lives here).

| PP-### | Statement | Status | Proposed solution | Resolved by | Feature |
|--------|-----------|--------|--------------------|--------------|---------|

## Changelog
- (YYYY-MM-DD) — register created
