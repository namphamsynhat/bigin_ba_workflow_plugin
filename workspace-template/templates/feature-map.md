---
type: feature-map
status: draft
version: 1.0
updated:
---

# Feature Map

One row per feature — the canonical list everything anchors to. `Slug` is permanent: every FR/PRD/EP/US/UX carries it in `feature:` frontmatter, and `/bigin-transform-signal` refuses to create an FR/BR whose slug isn't here.

Feature statuses: `proposed | committed | not-built | built | out-of-scope`. Agents only set `proposed`; humans set the rest. Never delete a row — set `out-of-scope` with a changelog entry.

| Slug | Feature | Status | FR | Code areas | Sources | Notes |
|------|---------|--------|----|------------|---------|-------|
|      |         |        |    |            |         |       |

## Changelog
- 1.0 (YYYY-MM-DD) — initial map
